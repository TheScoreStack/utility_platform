// Domain entity types live in @utility-platform/shared so the API and the
// web app can't drift apart. Add API-only types below; add anything the web
// app also consumes to packages/shared instead.
export * from "@utility-platform/shared";
