{
  "name": "group-expenses-infra",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "synth": "cdk synth",
    "deploy": "cdk deploy",
    "diff": "cdk diff",
    "destroy": "cdk destroy"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.257.0",
    "constructs": "^10.3.0",
    "source-map-support": "^0.5.21"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "esbuild": "^0.28.0",
    "ts-node": "^10.9.2",
    "tsx": "^4.20.6",
    "typescript": "^5.4.2"
  },
  "comments": {
    "knownVulns": "aws-cdk-lib bundles brace-expansion 5.0.5 (moderate DoS via large numeric range, GHSA-jxxr-4gwj-5jf2). Bundled deps can't be replaced via npm overrides; waiting on a CDK upstream bump. Dev-only (CDK is build-time tooling)."
  }
}
